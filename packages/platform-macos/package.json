{
  "name": "@paneform/wm-macos",
  "private": true,
  "version": "2.0.0-alpha.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && swift build --package-path sidecar",
    "typecheck": "tsc --noEmit",
    "test": "swift test --package-path sidecar && vitest run",
    "lint": "oxlint src test"
  },
  "dependencies": {
    "@paneform/layout": "workspace:*",
    "effect": "3.22.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "oxlint": "1.80.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.11"
  },
  "exports": {
    ".": "./src/host.ts"
  }
}
